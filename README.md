# elevate-lab-task-8
for the the mall customer segmentation dataset  following is done:-
1.Loaded and visualize dataset (optional PCA for 2D view).
2.Fit K-Means and assign cluster labels.
3.Used the Elbow Method to find optimal K.
4.Visualized clusters with color-coding.
5.Evaluated clustering using Silhouette Score.
the tools used:-Scikit-learn, Pandas, Matplotlib
